# Code Generated by Sidekick is for learning and experimentation purposes only.
name: Test Email Notification

on:
  push:
    branches:
      - main

jobs:
  check_job:
    runs-on: ubuntu-latest
    steps:
      - name: Check Something
        run: |
          echo "Simulating failure"
          exit 1

      - name: Send deployment failure email
        if: failure()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587
          username: ${{ secrets.SMTP_USERNAME }}
          password: ${{ secrets.SMTP_PASSWORD }}
          subject: "🚨 Deployment Failed: ${{ github.workflow }}"
          body: |
            The deployment workflow **${{ github.workflow }}** failed.
            Repository: **${{ github.repository }}**
            Commit: ${{ github.sha }}
            Actor: ${{ github.actor }}
            Run: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
            Please review the workflow logs for more details.
          to: "kturakannavar@deloitte.com"
          from: "komalgt@gmail.com.com"
          secure: false # <-- Set to false for port 587
